// We suggest to put common modules, whose goal is to do special feature and tightly binds with the application, into `lib` directory.
// Other modules that have abilities to reuse in other application should be put into the `node_modules` .
