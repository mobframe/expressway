// `app/lib/dao` contains database access wrapper.
// e.g.
// `app/lib/dao/demoData.js` include functions connecting to the mysql which stores demo data. 
// However, the common connect-mysql-lib is in the `node_modules`.  
